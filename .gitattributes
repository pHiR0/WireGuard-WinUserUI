# Archivos privados — nunca se fusionan desde dev → main
# "merge=ours" hace que en un merge, main siempre gane (= ignorar el archivo entrante)
docs/ToDo.md            merge=ours
docs/ChuletasPrompts.md merge=ours
